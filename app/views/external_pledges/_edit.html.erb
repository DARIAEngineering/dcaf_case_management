<% unless external_pledge.new_record? %>
  <%= bootstrap_form_with model: external_pledge, url: patient_external_pledge_path(patient, external_pledge), remote: true do |f| %>
    <%= f.number_field :amount, control_class: 'form-control external_pledge_amount', prepend: '$', label: t('external_pledge.edit.fund_pledge', source: "#{external_pledge.source}") %>
  <% end %>
<% end %>
