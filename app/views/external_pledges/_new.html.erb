<h5>Or, record a new external pledge:</h4>
<%= bootstrap_form_for [patient, external_pledge] do |f| %><%# should be remote: true %>
  <%= f.select :source, options_for_select(available_pledge_source_options_for(patient)) %>
  <%= f.text_field :amount %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
