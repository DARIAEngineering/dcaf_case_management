<h5>Or, record a new external pledge:</h5>
<%= bootstrap_form_for [new_patient, new_external_pledge], remote: true do |f| %>
	<%= f.select :source, available_pledge_source_options_for(new_patient) %>
	<%= f.text_field :amount %>
	<%= f.submit class: 'btn btn-primary' %>
<% end %>