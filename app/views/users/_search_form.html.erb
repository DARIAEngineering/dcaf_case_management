  <%= bootstrap_form_tag url: users_search_path, remote: true, layout: :inline do |p| %>
    <%= p.text_field :search, placeholder: 'Name or Email', hide_label: true, id: 'search_field', class: 'search_form_input' %>
    <%= p.submit 'Search', id: 'search_button', class: 'btn btn-primary' %>
  <% end %>
