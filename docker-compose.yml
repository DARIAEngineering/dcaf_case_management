web:
  build: .
  command: rails s -p 3000 -b 0.0.0.0
  volumes:
    - .:/usr/src/app
  ports:
    - "3000:3000"
  links:
    - db
  environment:
    YR_VAR_HERE: thing
db:
  image: mongo
  volumes:
    - data/mongodb:/data/db
  ports:
    - "27017:27017"